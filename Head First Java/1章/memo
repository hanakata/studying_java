ソースファイルはクラスから構成される。
クラスはメソッドから構成される。
メソッドはステートメントから構成される。
プログラムは指定されたクラスを実行する際、まずはmainというメソッドを探す。
mainメソッドはアプリケーション単位でひとつ。クラス単位でひとつではない。

Javaがクラスで構成されるのはオブジェクト指向言語だから。
オブジェクト＝クラス

mainメソッドはアプリケーションごとにひとつだが、
テストなどによって実行場所を変えたいときは複数指定できる。
ただしクラスに対して1つのみ。
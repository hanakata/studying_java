メモリに関しては2種類。
ヒープとスタック。
スタックはメソッドやローカル変数が格納される
ヒープはオブジェクトが格納される。

変数の場合、ローカル変数はスタックに保存される。
インスタンス変数はヒープに格納される。

メソッドは呼び出されるとスタックに格納される。スタックはスタックフレームという単位で格納される。
スタックフレームには個々のメソッドとローカル変数の値が含まれる。
スタックの最上部には実行中のメソッドが保存される。

コンストラクタを使用することで初期値を設定することが出来るので
安全なプログラムになる。
コンストラクタに利用できるのはthisかsuperのどちらかだけ。
